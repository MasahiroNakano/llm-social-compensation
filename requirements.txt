# PyTorch is intentionally omitted.
# Run this repository inside a RunPod PyTorch pod so the CUDA-enabled
# PyTorch build supplied by the pod remains untouched.
# Qwen3.5 requires Transformers 5.3+. Pin 5.13.1 because 5.14 introduced a
# DTensor import that is incompatible with PyTorch 2.4.1 in the RunPod image.
transformers==5.13.1
accelerate>=1.0.0,<2.0.0
tqdm>=4.66.0,<5.0.0
