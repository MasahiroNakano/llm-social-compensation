[project]
name = "llm-social-compensation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.14.0",
    "safetensors>=0.8.0",
    "transformers>=5.16.1",
]

[project.scripts]
llm-social-compensation = "llm_social_compensation:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.6",
]
